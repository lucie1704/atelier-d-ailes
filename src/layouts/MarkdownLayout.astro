---
import Layout from '~/layouts/PageLayout.astro';
import Hero from '~/components/widgets/Hero.astro';

import type { MetaData } from '~/types';
import Markdown from '~/components/ui/Markdown.astro';
import BreadcrumbsSEO from '~/components/common/BreadcrumbsSEO.astro';

export interface Props {
  frontmatter: {
    title: string;
    tagline?: string;
    subtitle?: string;
  };
  content: string;
  breadcrumbs?: { name: string; url: string }[];
}

const { frontmatter, content, breadcrumbs } = Astro.props;

const metadata: MetaData = {
  title: frontmatter?.title,
};
---

<Layout metadata={metadata}>
  {breadcrumbs && <BreadcrumbsSEO items={breadcrumbs} />}

  <Hero title={frontmatter.title} tagline={frontmatter.tagline} subtitle={frontmatter.subtitle} />

  <section class="p-4 mx-auto max-w-4xl mb-16 pb-16">
    <Markdown content={content} />
  </section>
</Layout>
